/**
 * DashboardLayout is no longer used as a route wrapper.
 * The AppLayout sidebar provides navigation to individual dashboard pages.
 * This file is kept for backward compatibility but can be removed.
 */
export function DashboardLayout() {
  return null;
}
